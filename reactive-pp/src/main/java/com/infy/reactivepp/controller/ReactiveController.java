package com.infy.reactivepp.controller;

/**
 * Created by aditya on 21/1/18.
 */
//@RestController
//@RequestMapping("/movies")
public class ReactiveController {
    /*private final MovieService movieService;

    public ReactiveController(MovieService movieService) {
        this.movieService = movieService;
    }

    @GetMapping
    public Flux<Movie> getMovies() {
        return this.movieService.getAllMovies();
    }

    @GetMapping("/{id}")
    public Mono<Movie> getMovie(@PathVariable String id) {
        return this.movieService.getMovieById(id);
    }

    @GetMapping(value = "/{id}/events", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
    public Flux<MovieEvent> getEvents(@PathVariable String id) {
        return this.movieService.geEvents(id);
    }*/
}
